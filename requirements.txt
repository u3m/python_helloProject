# XGBoost
# requirements.txt
# install
# pip config set global.index-url https://mirrors.aliyun.com/pypi/simple/
# pip install -r requirements.txt

wheel == 0.36.2
NumPy == 1.19.5 # 配合 scikit
SciPy == 1.6.0 # 配合 scikit
joblib == 1.0.0 # 配合 scikit
scikit-learn == 0.24.1 # scikit-learn
xgboost == 1.3.3 # XGBoost
PyMySQL == 1.0.2 # 连接数据库
xlrd == 2.0.1 # 读取xls表格使用
matplotlib == 3.3.3 # 画线使用
flask == 1.1.2 # Web微框架